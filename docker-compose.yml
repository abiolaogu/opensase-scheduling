version: '3.8'
services:
  scheduling:
    build: .
    ports:
      - "8088:8088"
    environment:
      - DATABASE_URL=postgres://postgres:postgres@db:5432/scheduling
      - NATS_URL=nats://nats:4222
      - RUST_LOG=debug
      - PORT=8088
    depends_on:
      db:
        condition: service_healthy
    networks:
      - scheduling-network

  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=scheduling
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - scheduling-network

  nats:
    image: nats:2.10-alpine
    command: ["--js"]
    networks:
      - scheduling-network

volumes:
  postgres_data:

networks:
  scheduling-network:
    driver: bridge
